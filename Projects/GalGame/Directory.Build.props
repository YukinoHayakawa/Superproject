<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup>
    <Dir_IncludePath>$(SolutionDir)Projects\GalGame;$(Dir_IncludePath)</Dir_IncludePath>
  </PropertyGroup>
</Project>